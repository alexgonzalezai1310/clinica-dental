{
  // Config que Nitro fusiona con el wrangler.json que genera en cada build
  // (ver .output/server/wrangler.json). "main" y "assets" los rellena Nitro;
  // "name" fija el nombre del Worker en Cloudflare.
  "name": "clinica-dental",
  "ai": {
    "binding": "AI",
  },
}
